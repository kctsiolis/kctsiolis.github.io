title: KC Tsiolis
email: kc.tsiolis@mail.utoronto.ca
description: >- # this means to ignore newlines until "baseurl:"
  Ph.D. Student in Statistics at the University of Toronto and the Vector Institute
baseurl: "" # the subpath of your site, e.g. /blog
url: "localhost:4000" # the base hostname & protocol for your site, e.g. http://example.com
github_username:  kctsiolis
linkedin_username:  kctsiolis
twitter_username: kctsiolis

# Build settings
remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
gems:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock

logo: bio_pic.jpg

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
